import { fontAwesome } from '../../../helpers/font-awesome';
import { module, test } from 'qunit';

module('Unit | Helper | font awesome');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = fontAwesome(42);
  assert.ok(result);
});
